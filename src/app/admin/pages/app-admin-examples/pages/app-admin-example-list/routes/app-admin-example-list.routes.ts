import { generateRoutes } from "src/app/shared/helpers/routes";

const BASE_PATH = "/admin/examples/list";
const BASE_ROUTES = [];

export const APP_ADMIN_EXAMPLE_LIST_ROUTES = generateRoutes(BASE_PATH, BASE_ROUTES);
