import { generateRoutes } from "src/app/shared/helpers/routes";

const BASE_PATH = "/admin/examples/calendar";
const BASE_ROUTES = [];

export const APP_ADMIN_EXAMPLE_CALENDAR_ROUTES = generateRoutes(BASE_PATH, BASE_ROUTES);
