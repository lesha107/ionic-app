import { generateRoutes } from "src/app/shared/helpers/routes";

const BASE_PATH = "/admin/examples/skeleton";
const BASE_ROUTES = [];

export const APP_ADMIN_EXAMPLE_SKELETON_ROUTES = generateRoutes(BASE_PATH, BASE_ROUTES);
