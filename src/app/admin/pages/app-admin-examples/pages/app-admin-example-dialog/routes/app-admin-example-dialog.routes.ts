import { generateRoutes } from "src/app/shared/helpers/routes";

const BASE_PATH = "/admin/examples/dialog";
const BASE_ROUTES = [];

export const APP_ADMIN_EXAMPLE_DIALOG_ROUTES = generateRoutes(BASE_PATH, BASE_ROUTES);
