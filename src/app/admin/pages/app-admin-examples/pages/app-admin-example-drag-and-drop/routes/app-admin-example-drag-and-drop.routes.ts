import { generateRoutes } from "src/app/shared/helpers/routes";

const BASE_PATH = "/admin/examples/drag_and_drop";
const BASE_ROUTES = [];

export const APP_ADMIN_EXAMPLE_DRAG_AND_DROP_ROUTES = generateRoutes(BASE_PATH, BASE_ROUTES);
