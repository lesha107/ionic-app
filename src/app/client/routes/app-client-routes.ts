import { routesTypeDefinition } from "src/app/shared/helpers/routes";

const BASE_PATH = "client";

export const APP_CLIENT_ROUTES = routesTypeDefinition({
  BASE: {
    path: "",
    fullPath: BASE_PATH
  }
});
